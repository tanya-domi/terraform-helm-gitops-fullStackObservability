name: CI loadgenerator

permissions:
  contents: write
  id-token: write
  pull-requests: write

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "apps/loadgenerator/**"
      - "charts/microservices/loadgenerator/**"
      - ".github/workflows/reusable-service-ci.yml"
      - ".github/workflows/ci-loadgenerator.yml"

jobs:
  ci:
    uses: ./.github/workflows/reusable-service-ci.yml
    with:
      service: loadgenerator
    secrets: inherit
